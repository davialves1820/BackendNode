const { resolve } = require("path");

module.exports = {
  config: resolve("src", "config", "database.js"),
  modelsPath: resolve("src", "app", "models"),
  migrationsPath: resolve("src", "database", "migrations"),
  seedersPath: resolve("src", "database", "seeders"),
};
